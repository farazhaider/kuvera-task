# Kuvera-task FrontEnd

Made in **Vue2.x** using **Axios** to make network calls.

Run these steps in order.

    npm install
    
    npm run serve
   
This will install all the dependencies and start the development server.